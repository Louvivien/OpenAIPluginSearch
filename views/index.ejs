<!DOCTYPE html>
<html>


<head>
    <title>OpenAI Plugin Search</title>
    <meta name="description" content="Search for OpenAI plugins. You can search by plugin name or by a keyword related to the plugin's functionality.">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .loader {
            border: 10px solid #f3f3f3;
            border-top: 10px solid #3498db;
            border-radius: 50%;
            width: 30px;
            height: 30px;
            animation: spin 2s linear infinite;
            margin: 20px auto;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="mt-5">OpenAI Plugin Search</h1>
        <p class="mt-3">Enter a query to search for OpenAI plugins. You can search by plugin name or by a keyword related to the plugin's functionality.</p>
        <form action="/search" method="get" class="mt-3">
            <input type="text" name="q" class="form-control" placeholder="Enter your query">
            <div class="loader" id="loader" style="display: none;"></div>
            <button type="submit" class="btn btn-primary mt-3">Search</button>
        </form>
    </div>
    <script>
        const form = document.querySelector('form');
        const loader = document.getElementById('loader');

        form.addEventListener('submit', () => {
            loader.style.display = 'block';
        });
    </script>
</body>
</html>
